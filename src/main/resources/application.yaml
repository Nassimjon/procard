spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url:  jdbc:postgresql://localhost:5432/procard?useSSL=false
    username: root
    password: root

  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
server:
  port: 8082
  servlet:
    multipart:
      max-file-size: 128KB
      max-request-size: 128KB